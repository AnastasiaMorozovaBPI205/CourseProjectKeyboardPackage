    import XCTest
    @testable import CourseProjectKeyboardPackage

    final class CourseProjectKeyboardPackageTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(CourseProjectKeyboardPackage().text, "Hello, World!")
        }
    }
